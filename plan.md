# Invisible Maze 풀스펙 구현 계획

## Summary

- Next.js + Supabase + Vercel 기준으로 2인 온라인 대전 게임을 처음부터 구현한다.
- 익명 세션으로 즉시 플레이 가능하게 하고, Google OAuth로 계정 연결을 지원한다.
- 자동 매칭과 방 코드 초대를 모두 제공하며, 게임 상태는 서버 권위적으로 관리한다.
- UI는 i18n 구조를 두고 한국어 문구를 기본으로 작성한다.

## Key Changes

- Next.js 앱을 새로 구성하고 `pnpm`, GitHub CI, Vercel 배포 기준 환경 변수를 정리한다.
- Supabase 스키마를 설계한다: 사용자/세션, 매치 큐, 방, 게임, 플레이어 상태, 이벤트 로그.
- Supabase Realtime으로 방 대기, 매칭, 턴 진행, 접속 상태를 동기화한다.
- Cloudflare Turnstile은 매치 큐 참가와 방 생성/참가 전에 서버 검증한다.
- `status.md`를 만들고 각 작업 단위마다 진행 내역을 이어서 기록한다.

## Game Rules

- 보드는 8x8이며 좌표 기준은 `(0,0)`이 화면 기준 왼쪽 아래다.
- 플레이어 A는 `(0,0)`에서 `(7,7)`, 플레이어 B는 `(0,7)`에서 `(7,0)`으로 이동한다.
- 미로는 칸 사이 벽으로 생성하며, 양 플레이어의 시작점-목표점 최단거리 길이가 같아야 한다.
- 선공은 랜덤이며 게임 시작 전 동전 던지기 애니메이션을 보여준다.
- 각 턴은 30초 안에 정확히 3스텝을 입력해야 한다.
- 목적지에 도착하면 남은 스텝과 무관하게 즉시 승리한다.
- 벽에 부딪히면 해당 벽을 양쪽 플레이어에게 잠깐 보여주고, 플레이어는 그 턴 시작 위치로 돌아가며 턴이 끝난다.
- 시간 초과는 턴 스킵으로 처리하고, 3회 연속 무입력/시간 초과 시 패배 처리한다.
- 플레이어끼리 같은 칸에 겹치는 것은 허용한다.
- 상대 이탈은 60초 유예 후 몰수패 처리한다.

## Public Interfaces

- Route handlers/server actions:
  - Turnstile 검증
  - 익명 세션 생성/복구
  - Google OAuth 계정 연결
  - 자동 매칭 큐 참가/취소
  - 방 코드 생성/참가
  - 게임 액션 제출
  - 재접속 상태 복구
- Realtime channels:
  - 온라인 사용자 수
  - 매치 큐/방 상태
  - 게임별 턴 상태와 이벤트
- Event log:
  - append-only JSONL 형태로 이동, 벽 충돌, 턴 스킵, 재접속, 승리/패배 이벤트를 저장한다.
  - 현재 게임 상태는 별도 정규화된 DB row에 보관한다.

## UI

- 첫 화면은 실제 플레이 진입 화면으로 구성한다: 온라인 수, 자동 매칭, 방 코드 생성/참가, 계정 연결 상태.
- 게임 화면은 모바일과 데스크톱 모두 지원한다.
- 터치 조작과 키보드 방향키 입력을 모두 지원한다.
- 보이지 않는 미로 특성을 유지하되, 벽 충돌 시 처음 벽을 보여주고, 10초간 서서히 사라지게 한 후 이후에는 완전히 사라지게 한다.
- 진행 중 게임은 비공개이며 관전 기능은 만들지 않는다.

## Test Plan

- 미로 생성 테스트: 경로 존재, 양쪽 최단거리 동일, 벽 충돌 판정.
- 게임 규칙 테스트: 3스텝 처리, 목적지 즉시 승리, 벽 충돌 후 복귀, 턴 스킵, 3회 무입력 패배.
- 서버 권위 테스트: 잘못된 턴/잘못된 플레이어/불가능한 이동 거부.
- 매칭 테스트: 자동 큐 매칭, 방 코드 참가, 큐 취소, 동시 요청 충돌 방지.
- 재접속 테스트: 익명 세션 복구, 게임 중 새로고침, 이탈 후 60초 내 복귀.
- E2E 테스트: 두 브라우저 세션으로 매칭부터 승리까지 진행.
- CI에서 타입체크, 린트, 단위 테스트를 실행한다.

## Assumptions

- Supabase 프로젝트와 Vercel 프로젝트는 사용자가 별도로 생성하고, 구현은 필요한 env 이름과 설정 절차를 문서화한다.
- Google OAuth는 Supabase Auth provider 설정을 전제로 한다.
- Turnstile site key/secret은 환경 변수로 주입한다.
- 첫 버전에는 관전, 리플레이 공유, 봇 대체 플레이어, 랭킹 시스템은 포함하지 않는다.
